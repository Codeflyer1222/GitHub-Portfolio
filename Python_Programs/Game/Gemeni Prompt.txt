I'm storing each word combination in a csv file. For instance, I'm using the word "study" and in its csv file it has 10 word combinations. I think I should also use the word "grade" and "learn", what do you think? Every time the game is played it will be comparing the user's score to the Score_Card.csv file in a loop. I'll add an if statement to create an exception if the csv only has 1 row. 


So should I put the TopLevel widget and attribute in the BrownS_WordGame file? How should I load the meme image into the widget? I put the back end processing in my Game_Engine file, but now the tkinter stuff like entry_box_one and result_label are showing errors. What's the problem and how do I fix it? Also help me further understand what an API is and use the context of my game files as an example. Also, to answer your question, I would prefer to use a list because I don't know what a set is and my teacher didn't teach us about it this semester so I would prefer to not over complicate things. Also, to clarify, you are saying I need to think about the information the Game_Engine.py file needs to inherently know from the csv files and create some methods for moving data from the Game_Engine.py file to the BrownS_WordGame.py file? Other side note, do I need to have the tikinter libraries in my Game_Engine.py file? I would prefer to not use code I don't need.

Ok I made an adjustment to the Game_Engine file. Does it look apropriate? I'm trying to make a way to retrieve data from the GUI so it can be calculated in my methods and then write the methods in the GUI so it can present the calculated response to the player/user. I have another problem, when the player submits an answer (done in the submit_answer method), it will check if the answer has already been used in the already_guessed list, which gets updated every time a player submits a correct answer. However, the already_guessed list appears to become empty when the player submits the answer again. How do I make the list maintain its data while the program is ready? Should I place already_guessed outside of the submit_answer method? Should I also place player_scores outside of the announce_winner method? Another issue is I'm trying to present the first word of the randomly selected word_list csv. However, I'm not sure if using Back_End.self.main_word is the right way to do it? I also want to create a pop-up window for the player to enter his/her player name. What is the best way to do that in my GUI file? Another question, do I need to add the player's score when he/she creates their name in the create_player_name method? Also, from my perspective now I need two button for a user to either login or create a player name at the beginning of the game. Or am I overthinking it? I don't want to accidentally create multiple accounts of the same player name every time they open the game.

I use get() several times in my code. Am I using it correctly like in the create_new_player and retrieve_player_list methods?

why is the current_player_score_label configuration posing an error in my BrownS_WordGame file?

So I managed to get the game word to load up from the start_game method and display on screen, but it's showing a dictionary pair of the game word with an acceptable guess word because the list_of_words is a list of dictionaries. Is it possible to just contain every word of the csv file in a list without using a dictionary?

Based on what I have written so far, do you think I could get rid of self.current_player_name and just use the player_name parameter? Also, am I using the best way to display the top five players and their high scores?